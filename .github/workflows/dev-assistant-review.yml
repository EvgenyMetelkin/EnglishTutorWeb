name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  ai-review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Deps
        run: npm ci

      - name: Build
        run: npm run build

      - name: Build RAG index
        if: vars.LLM_PROVIDER && secrets.LLM_API_KEY
        run: npx dev-assistant index
        env:
          LLM_PROVIDER: ${{ vars.LLM_PROVIDER || 'openai' }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_EMBED_MODEL: nomic-embed-text:latest

      - name: Run AI review
        id: review
        if: vars.LLM_PROVIDER && secrets.LLM_API_KEY
        run: |
          REVIEW=$(npx dev-assistant review-pr \
            --base ${{ github.event.pull_request.base.ref }} \
            --format markdown)
          echo "review<<EOF" >> $GITHUB_OUTPUT
          echo "$REVIEW" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        env:
          LLM_PROVIDER: ${{ vars.LLM_PROVIDER || 'openai' }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL || 'gpt-4o' }}

      - name: Post review comment
        if: vars.LLM_PROVIDER && secrets.LLM_API_KEY && steps.review.outputs.review
        uses: actions/github-script@v7
        with:
          script: |
            const review = `${{ steps.review.outputs.review }}`;
            if (!review || review === 'No changes to review.') return;

            github.rest.issues.createComment({
              ...context.repo,
              issue_number: context.issue.number,
              body: review,
            });
